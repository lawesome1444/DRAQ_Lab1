//2(b) Arrow function accepting variables
//When called, this function takes the passed value, assigns it to "argument" and then prints argument
//Done to make the function more compact
let passedVarArrowFunc = (argument)=>{
    console.log(argument);
}
//Pass the long string of text to the arrow function above so it can be printed to console
passedVarArrowFunc("This will be passed to the varDataRep Arrow function!");
